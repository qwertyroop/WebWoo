/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.pillarstack.com', 'placehold.co', 'ik.imagekit.io', 'repository-images.githubusercontent.com','bashooka.com', 'ph-files.imgix.net', 'miro.medium.com'],
    },
};

export default nextConfig;